{
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "jsdom": "^29.0.1"
  },
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install --include=dev && VITE_API_BASE_URL= npm run build"
  }
}
